apiVersion: v1
kind: Service
metadata:
    name: "akskodey-e976"
    labels:
        app: "akskodey-e976"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akskodey-e976"
