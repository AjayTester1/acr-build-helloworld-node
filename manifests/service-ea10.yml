apiVersion: v1
kind: Service
metadata:
    name: "akskodey-ea10"
    labels:
        app: "akskodey-ea10"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akskodey-ea10"
