apiVersion: v1
kind: Service
metadata:
    name: "aks1kodey-2cdc"
    labels:
        app: "aks1kodey-2cdc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks1kodey-2cdc"