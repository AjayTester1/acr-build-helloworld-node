apiVersion: v1
kind: Service
metadata:
    name: "akskodey-c40d"
    labels:
        app: "akskodey-c40d"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akskodey-c40d"