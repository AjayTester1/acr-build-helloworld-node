apiVersion: v1
kind: Service
metadata:
    name: "akskodey-868e"
    labels:
        app: "akskodey-868e"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akskodey-868e"
