apiVersion: v1
kind: Service
metadata:
    name: "aks1kodey-cd53"
    labels:
        app: "aks1kodey-cd53"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "aks1kodey-cd53"